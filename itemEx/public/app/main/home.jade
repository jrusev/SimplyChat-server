.container
    .jumbotron
        #logo-title.h1 ItemEx
        h2 The best deals for used items
    .row
        .col-md-offset-3.col-md-6
            form.navbar-form(role="search")
            .input-group

                input.form-control(type="text" placeholder="Search" name="q" ng-model="filters.query" ng-keyup="$event.keyCode == 13 && findItem(filters.query)")
                .input-group-btn
                    button.btn.btn-default(ng-click="findItem(filters.query)")
                        i.glyphicon.glyphicon-search

    br
    .row
        .col-md-12
            h1.text-center Featured
            div(items-grid items="featuredItems"  num="num")

    .row
        .col-md-12
            h1.text-center Latest
            div(items-grid items="latestItems"  num="num")
