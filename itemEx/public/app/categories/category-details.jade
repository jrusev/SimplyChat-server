.container
    .row.text-center
        h1 {{title}}
    .row
        .col-md-4
            form.navbar-form(role="search")
            .input-group
                input.form-control(type="text" placeholder="Search" name="q" ng-model="filters.query" ng-keyup="$event.keyCode == 13 && findItems()")
                .input-group-btn
                    button.btn.btn-default(ng-click="findItems()")
                        i.glyphicon.glyphicon-search

        .col-md-4
            form.navbar-form(role="search")

                select.form-control(ng-model="filters.orderBy" ng-change="findItems()")
                    option(value="-published" ng-selected="") Newest
                    option(value="published") Oldest
                    option(value="price") Low price
                    option(value="-price") High price
                    option(value="title") Title
     br
    .row

        .col-md-3
            .list-group
                a.list-group-item(href="#/categories/{{category.name}}" ng-repeat="category in categories | orderBy: 'name'") {{category.name}}

        .col-md-9
            div(data-pagination="" data-num-pages="numPages()" data-current-page="currentPage" data-max-size="maxSize" data-boundary-links="true")
            div(items-grid items="items" num="numPerPage")
            div(data-pagination="" data-num-pages="numPages()" data-current-page="currentPage" data-max-size="maxSize" data-boundary-links="true")